import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "it.emporiocase.gestionale",
    appName: "Emporio 23",
    webDir: "build",
    bundledWebRuntime: false,
};

export default config;
