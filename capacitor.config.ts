import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "it.emporiocase.nativeapp",
    appName: "Emporio Case",
    webDir: "build",
    bundledWebRuntime: false,
};

export default config;
